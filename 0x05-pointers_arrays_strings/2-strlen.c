#include "main.h"
/**
 * *_strlen - returns the length of a string
 * @s: string
 * retrn: length
 * */
int _strlen(char 8s)
{ 
	int longi = 0;

	while (*s != '\0')
	{
		longi++;
		s++;
	}

	return (longi);
}
